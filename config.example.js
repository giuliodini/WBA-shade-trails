// ShadeTrails — sensitive configuration template
// Copy this file to config.js and fill in your API key.
// config.js should be added to .gitignore so it is never committed.

// ShadeMap API key — request one at https://shademap.app
// The key must be whitelisted for your domain (email api@shademap.app).
const SHADEMAP_API_KEY = 'YOUR_SHADEMAP_API_KEY';
